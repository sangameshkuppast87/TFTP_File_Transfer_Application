# TFTP_File_Transfer_Application
This project implements a basic TFTP (Trivial File Transfer Protocol) application using C and UDP sockets. The system consists of a client and server that communicate using the TFTP protocol to transfer files over a network.
